{
  "name": "clip8dependencies",
  "version": "0.1.0",
  "description": "all ext. dependencies needed for clip_8",
  "main": "index.js",
  "scripts": {
    "build": "browserify index.js > clip8dependencies.js"
  },
  "author": "",
  "license": "UNLICENCED",
  "dependencies": {
    "svgpath": "^2.2.1",
  },
  "devDependencies": {
    "browserify": "^14.1.0"
  }
}
